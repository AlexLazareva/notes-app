import React from 'react';

const ColorPicker = React.createClass(
    {

        render() {
            return (
                <div>
                    ColorPicker
                </div>
            );
        }
    }
);

export default ColorPicker;
