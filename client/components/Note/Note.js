import React from 'react';

import './styles.scss';

const Note = React.createClass({
    render() {
        return (
            <div>
                Note
            </div>
        );
    }
});

export default Note;